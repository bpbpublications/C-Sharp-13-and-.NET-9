var myExpression = 1;
var myValue1 = 1;
var myValue2 = 2; 

switch (myExpression)
{
    case myValue1:
        // Code to be executed if myExpression matches myValue1
        break;
    case myValue2:
        // Code to be executed if myExpression matches myValue2
        break;
    // ...
    default:
        // Code to be executed if myExpression doesn't match any case
        break;
}